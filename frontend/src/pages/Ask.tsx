import React from 'react';
import ChatBox from '../components/ChatBox';

const Ask: React.FC = () => {
    return (
        <div style={{ padding: '2rem' }}>
            <ChatBox />
        </div>
    );
};

export default Ask;
